
// This file re-exports toast functionality
import { useToast, toast, ToastProps, ToastActionElement } from "@/components/ui/use-toast";

// Export the hook and functions
export { useToast, toast };

// Re-export the types
export type { ToastProps, ToastActionElement };
